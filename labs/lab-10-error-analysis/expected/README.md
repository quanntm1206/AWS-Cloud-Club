# Kết quả tham khảo - lab-10-error-analysis

## Oracle

Review theo sampling rule, kèm support; đề xuất một data fix và một model fix.

## Required receipt

- Chạy `python scripts/run_lab.py --lab 10` từ repository root; PowerShell/Bash đầy đủ nằm trong README.
- JSON phải có `status=starter-example-completed`; trong `result` cần thấy slice metrics, failure records có cap và taxonomy.
- Learning log cục bộ ghi seed/config, runtime, phép kiểm riêng của lab và ít nhất một limitation hoặc failure.
- Tự trả lời: “Output này chứng minh được gì, và chưa chứng minh được gì?”

## Gợi ý nếu kết quả khác

Nếu không thấy pattern, tăng tính đa dạng mẫu lỗi thay vì chỉ chọn confident-wrong.

Đây là gợi ý tự kiểm, không phải bài cần gửi. Không commit evidence, secret, dữ liệu cá nhân, raw dataset lớn
hoặc output cloud trả phí.
